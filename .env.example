# Server Port
PORT=3000

# Admin dashboard secret (View Results / Excel / CSV on /admin.html)
SECRET_CODE=RISHAV1306

# A random string to secure the user sessions
SESSION_SECRET=a_very_long_random_string_here

# Optional: Set to production for better performance
NODE_ENV=production

# MongoDB Connection String (defaults to local if not set)
MONGODB_URI=mongodb://127.0.0.1:27017/gdg_quiz
